/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allows ls.clipotapp.com to serve as the short link domain
  // The [slug] catch-all handles redirects at the root level
}

module.exports = nextConfig
